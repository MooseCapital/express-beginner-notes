// const {WebSocket, WebSocketServer} = require('ws');

function websocketServerConfig(wss) {
    //when maxPayLoad is reached, the connection is closed, 1mb




}

module.exports = {websocketServerConfig}

